import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function AssetsPage() {
    return (
        <main className="flex flex-col gap-4 p-4">
            <h1>Stablecoin assets by network</h1>
            <Card>ethereum</Card>
        </main>
    );
}